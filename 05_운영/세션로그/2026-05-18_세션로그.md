# 2026-05-18 세션로그 (16차)

## 작업 범위
Supabase PostgreSQL 연결 완료 + Story 037-038 구현

## 주요 성과

### Supabase 연결 확정
- 문제: `aws-0`(오류) → 대시보드에서 직접 확인 → `aws-1-ap-northeast-1.pooler.supabase.com:6543`
- Alembic env.py: 동기 psycopg2, parents[2] 경로 수정, transaction_per_migration=True
- 007 마이그레이션 중복 인덱스 제거 후 001→007 전체 성공
- Supabase PostgreSQL 17.6, 21개 테이블 생성

### Story 037 — FastAPI DB Runtime Integration
- `database/connection.py`: NullPool + statement_cache_size=0 (pgBouncer 호환)
- `database/db_service.py` 신규: upsert_employee/posting/applicant, ON CONFLICT DO UPDATE
- employees 라우터 전체 dual-write (JSON primary + Supabase secondary)
- `POST /api/v2/acquisition/db-sync` — JSON 전체 Supabase 일괄 동기화
- `GET /health/db` → {"status":"ok","db":"supabase"} 확인

### Story 038 — Context Radar 중복 탐지
- `scripts/context_radar.py`: find_duplicate_flows(), _score_pair(), _filter_symbols()
- `POST /api/v1/context-radar/duplicates` 엔드포인트
- ContextRadar.jsx "중복 탐지" 탭: DuplicatePairCard, risk 임계값/색상
- 155개 항목 기준 실제 발견:
  - applicants/router.py ↔ interviews/router.py (StatusUpdate 공유)
  - Dashboard.jsx ↔ RecruitAnalytics.jsx (차트 컴포넌트 중복)

## 커밋 목록
- `7cce1b7` fix: Supabase connection + Alembic migration
- `bac6b0a` feat: Story 037 — DB runtime integration
- `6bc611f` feat: Story 038 — Context Radar duplicate-flow heuristics
- `b2833e1`, `65f7924` docs updates

## 내일 이어할 작업
1. `POST /api/v2/acquisition/db-sync` 실행 → 기존 JSON 데이터 Supabase 업로드
2. Story 039: Employee/Posting CRUD DB-first 전환
3. applicants ↔ interviews StatusUpdate 중복 정리
4. JobKorea/Wanted 커넥터 연구
