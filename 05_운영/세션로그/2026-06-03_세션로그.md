# 2026-06-03 세션로그 (반다이나믹 워크플로우 원칙 + 토큰 게이지/자동백업 셋업)

> ※ 이 로그는 같은 날(2026-06-03) `hr-tools-web/SESSION_LOG.md`와 git 이력에서 **소급 정리**한 기록입니다.

## 세션 요약

제품 지침(CLAUDE.md)에 **원칙 6 반(半)다이나믹 워크플로우**를 추가하고, 개발 환경에 토큰 사용률 상태줄과 자동 압축 직전 자동백업 훅을 셋업했다.

핵심 커밋: `c14c6dd` 세션로그: 반다이나믹 워크플로우 원칙 6 + 토큰 게이지/PreCompact 자동백업 셋업 기록

---

### 1. 제품 지침 (CLAUDE.md)

- **원칙 6 추가 — "워크플로우는 반(半)다이나믹으로 설계한다"**
  - 정적(static) / 다이나믹(dynamic) / 반다이나믹(semi-dynamic) 3분류
  - AEON은 컴플라이언스(노무·법령) 영역이므로 **"AI가 다음 단계 제안 → 사람이 승인"** 게이트 필수
  - 헌법(Information→Event→Synapse→Workflow→Decision→Record→Learning)의 Workflow·Decision 단계가 이 원칙의 구현체
- **BMAD 섹션 보강**: BMAD가 knowledge를 Story/QA로 변환할 때, 분기 있는 모든 흐름에 "AI 제안 → 사람 승인" 게이트를 acceptance criteria로 이어받도록 명시. (원칙 6 ↔ BMAD 섹션 교차 참조 — BMAD가 원칙을 대체하지 않고 운영화)

### 2. 개발 환경 (~/.claude, 로컬 전용 — git 비대상)

- **statusLine 추가** (`statusline.ps1`): 입력창 하단에 컨텍스트 사용률 게이지 상시 표시.
  - 형식: `ctx 45% (89k/200k) | $0.42 | git:branch | model`
  - 60%↑ 노랑 / 85%↑ 빨강. 토큰은 transcript usage에서 계산
- **PreCompact 훅 (matcher: auto)**: 자동 압축 직전 AEON·hr-tools-web 두 저장소 자동 커밋+푸시. 기존 Stop 훅 로직 재사용, 메시지 "컨텍스트 압축 직전 자동 저장"으로 구분

### Notes

- `~/.claude`는 git 저장소가 아니라 settings.json/statusline.ps1은 로컬 머신에만 존재(푸시 대상 아님)
- 훅·상태줄은 다음 세션 재시작 후 적용
- `ultracode = xhigh effort + 다이나믹 워크플로우 자동 편성` (세션 스코프, `/effort ultracode`로 토글). 현재는 effortLevel xhigh까지만 설정

---

## 후속 (같은 날 이어진 작업)

- **옵시디언 볼트 backfill**: 5/30 오전 이후 멈춰 있던 AEON 볼트를 점검. git상 푸시 누락은 없었으나(두 저장소 모두 origin과 0/0 동기화), 세션로그가 5/26·5/27·5/31·6/3분 누락 → hr-tools-web 기록에서 소급 복원해 채움. 다이내믹 워크플로우(원칙 6: AI 제안 → 사람 승인) 가동으로 진행.
