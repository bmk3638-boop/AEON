# 2026-06-03 세션로그 (반다이나믹 워크플로우 원칙 + 토큰 게이지/자동백업 셋업)

> ※ 이 로그는 같은 날(2026-06-03) `hr-tools-web/SESSION_LOG.md`와 git 이력에서 **소급 정리**한 기록입니다.

## 세션 요약

제품 지침(CLAUDE.md)에 **원칙 6 반(半)다이나믹 워크플로우**를 추가하고, 개발 환경에 토큰 사용률 상태줄과 자동 압축 직전 자동백업 훅을 셋업했다.

핵심 커밋: `c14c6dd` 세션로그: 반다이나믹 워크플로우 원칙 6 + 토큰 게이지/PreCompact 자동백업 셋업 기록

---

### 1. 제품 지침 (CLAUDE.md)

- **원칙 6 추가 — "워크플로우는 반(半)다이나믹으로 설계한다"**
  - 정적(static) / 다이나믹(dynamic) / 반다이나믹(semi-dynamic) 3분류
  - AEON은 컴플라이언스(노무·법령) 영역이므로 **"AI가 다음 단계 제안 → 사람이 승인"** 게이트 필수
  - 헌법(Information→Event→Synapse→Workflow→Decision→Record→Learning)의 Workflow·Decision 단계가 이 원칙의 구현체
- **BMAD 섹션 보강**: BMAD가 knowledge를 Story/QA로 변환할 때, 분기 있는 모든 흐름에 "AI 제안 → 사람 승인" 게이트를 acceptance criteria로 이어받도록 명시. (원칙 6 ↔ BMAD 섹션 교차 참조 — BMAD가 원칙을 대체하지 않고 운영화)

### 2. 개발 환경 (~/.claude, 로컬 전용 — git 비대상)

- **statusLine 추가** (`statusline.ps1`): 입력창 하단에 컨텍스트 사용률 게이지 상시 표시.
  - 형식: `ctx 45% (89k/200k) | $0.42 | git:branch | model`
  - 60%↑ 노랑 / 85%↑ 빨강. 토큰은 transcript usage에서 계산
- **PreCompact 훅 (matcher: auto)**: 자동 압축 직전 AEON·hr-tools-web 두 저장소 자동 커밋+푸시. 기존 Stop 훅 로직 재사용, 메시지 "컨텍스트 압축 직전 자동 저장"으로 구분

### Notes

- `~/.claude`는 git 저장소가 아니라 settings.json/statusline.ps1은 로컬 머신에만 존재(푸시 대상 아님)
- 훅·상태줄은 다음 세션 재시작 후 적용
- `ultracode = xhigh effort + 다이나믹 워크플로우 자동 편성` (세션 스코프, `/effort ultracode`로 토글). 현재는 effortLevel xhigh까지만 설정

---

## 후속 (같은 날 이어진 작업)

- **옵시디언 볼트 backfill**: 5/30 오전 이후 멈춰 있던 AEON 볼트를 점검. git상 푸시 누락은 없었으나(두 저장소 모두 origin과 0/0 동기화), 세션로그가 5/26·5/27·5/31·6/3분 누락 → hr-tools-web 기록에서 소급 복원해 채움. 다이내믹 워크플로우(원칙 6: AI 제안 → 사람 승인) 가동으로 진행.

### HR OS 고도화 — Epic 0 기술부채 정리 (세션20)

다이내믹 워크플로우 + BMAD로 고도화 프로젝트 착수. FE/BE 진단(라우터 30·페이지 67·마커 291건) 후 E0부터 실행.

- **FE 정리** (commit `3fd8bdf`): 레거시 고아 페이지 8개 삭제(−4913줄), 죽은 버튼 5개 비활성화+안내, 라이브 mock에 "샘플" 배지, 🚧 스텁에 "준비 중" 배지. 빌드 클린(2392모듈).
- **BE 노무 DB 영속화** (commit `ee38859`): 서버 재시작 시 휘발되던 노조대응가이드·고충처리/인사위원회 위원을 DB 테이블로 이관(Alembic 016, Supabase 015→016). 별도 세션 영속성 스모크 통과 → 데이터 소실 위험 해소.
- origin/master push 완료. 다음 후보: E1 Synapse 자동전파(179·180), E2 컴플라이언스 알림(181~185), E3 QA 인프라, E4 Advisor 실데이터(174~176).

### HR OS 고도화 — E1~E4 + QA 실증방안 (세션20, 자동 진행 모드)

배명규 대표 지시로 이번 세션은 권장안 자동 채택(승인 게이트 위임). E1~E4 백엔드 + QA 설계 완료.

- **E1 Synapse 자동전파**: 성과→보상 후보 추천(179), Q12→이직위험→SeparationOS 라우팅(180). 둘 다 needs_approval=True(반다이나믹). 감사로그 기록.
- **E2 법정 자동검증**: `/compliance/auto-checks` — 연차·퇴직급여 14일·괴롭힘 처리기한·산안교육·4대보험 상실신고(181~185). 실 DB 쿼리 + 법령 조문·기한·권고액션.
- **E3 QA 인프라**: pytest 15케이스 전부 통과(앱 백엔드 첫 테스트 스위트). 계약유형 데이터모델 Alembic 017 작성(프로덕션 DB 적용은 승인 대기).
- **E4 Advisor 실데이터**: `/platform/lab/advisors` — Mercer/IBM/Gallup 정적 mock을 Agent Action 큐·QA runs 실수치로 대체. 데이터 없으면 '수집중' 정직 표기.
- **QA 실증방안 설계**: `docs/bmad/QA_VALIDATION_OPERATING_MODEL_v1.md` — "검증(우리가 안 틀렸다) vs 실증(고객에게 맞다)" 분리. QA 4단 피라미드, L4 실증 증거 4종, 반다이나믹 실증 파이프라인, R1~R5 로드맵. 설계자모드를 '기능 만드는 곳'이 아니라 '실증 통과 관문'으로 재정의.
- 검증: 전 라우터 임포트 OK, pytest 15/15. 미적용: Alembic 017(DB 승인 대기), FE 서피싱(다음 단계). origin push 완료.

#### 승인 후 자동 진행 (세션20-후속)

- **Alembic 017 적용 완료**(대표 승인): DB 016→017, Employee.employment_type 활성화 → 계약유형 추측 휴리스틱 제거 토대 완성
- **E3 FE**: ComplianceCenter 계약유형을 정식 필드 우선 사용(휴리스틱 폴백)
- **E2 FE**: ComplianceCenter에 법정 자동검증 패널 추가 — 5개 검증 카드(법령·기한·권고), 위험 클릭→OS 이동, 알림 발행
- **E4**: advisor 등급 단위테스트 추가(pytest 16/16). FE 연결은 AEONOrg 대형파일 위험으로 보류(다음 세션 verify 동반)
- 빌드 클린, master 동기화. 남은 일: E4·E1 FE 서피싱, L2 통합테스트, 분기 고객 파일럿(원칙5).

#### verify 동반 진행 (gstack 헤드리스 브라우저 실렌더 검증)

- E4·E1 FE 구현 후 **실제 앱 구동 + 로그인 + 브라우저 검증**까지 완료(대표 비번 제공).
- **E4 PASS**: `/aeon-hq` 고문단 → `/advisors` 200 → IBM 카드 "실시간 지표·실데이터"(Agent 27·승인대기 25·고위험 13·게이트 100%) 렌더, 스크린샷 확보.
- **E1 PASS**: `/relations/surveys` 이직위험 도구 → Q12 45+신호2 → `/retention-risk` 201 → "이직위험 높음·74점 + SeparationOS 라우팅 제안" 렌더, 스크린샷 확보.
- 의미: 이번 세션에 QA 실증 L1(pytest 16) + **L4 실증(실데이터·브라우저 렌더)** 일부를 직접 수행. 헌법 Record/원칙5 부합.
