# 2026-05-17 세션로그

## 세션 요약

이전 세션에서 완성된 Epic 9(채용 인사이트 보고서)와 처우승인 경력인정 연동을 커밋하고,
이어서 Story 1.3 테넌트 격리 미들웨어를 완성했다.

## 완료 작업

### [커밋] Epic 9 + 처우승인 경력인정 연동 (이전 세션 미커밋분)

- `AcqOfferApproval.jsx`: 경력인정 산정 매트릭스 통합, 이력서 PDF 미리보기, offerAmount 연동
- `AcqRecruitingReport.jsx`: 채용 인사이트 보고서 신규 페이지 (퍼널·채널·AI점수·처우 분석)
- App.jsx, acqNavigation.js, AppSidebar.jsx: 라우트·사이드바 등록

### [Story 1.3] 테넌트 격리 미들웨어 (Soft JWT 기반)

**Backend**

- `backend/auth/dependencies.py` 신규 생성
  - `get_current_tenant()`: JWT 이메일 → UUID5 테넌트 파생
  - 토큰 없을 시 `_DEV_TENANT` 폴백 (dev/demo 무중단)
- `modules/jobs/router.py`, `modules/skills/router.py`, `modules/talent/router.py`
  - 하드코딩 `_DEV_TENANT/_tenant()` 전면 제거
  - `Depends(get_current_tenant)` 주입 (30+ 엔드포인트)

**Frontend**

- `AcqCandidates.jsx`: `useAuthHeaders()` 훅 추가
  - 모든 talent/jobs/skills API 호출에 `Authorization: Bearer <token>` 헤더 적용
  - SkillAnalysisPanel, MatchingPanel, ComparePanel, InterviewPanel, RecognitionPanel 전체 적용

## 빌드

- Vite build ✓ (1799 modules, 2.14s)

---

## [Story 1.4] Audit Log — HR 결정 불변 이벤트 로그

**신규 파일**
- `backend/audit/models.py`: AuditLog 테이블 (tenant_id, actor_email, event_type, entity_type, entity_id, JSONB payload, created_at). Append-only.
- `backend/audit/service.py`: `log_event()` + `list_logs()`
- `backend/audit/router.py`: `GET /api/v1/audit/logs` (event_type/entity_type 필터, 최신순 페이지네이션)

**수정 파일**
- `auth/dependencies.py`: `get_current_actor()` 추가 (JWT email → "anonymous" 폴백)
- `talent/router.py`: 4개 HR 결정 이벤트 로깅 연결
  - `skill.verified` → HR 숙련도 수동 검증 시
  - `matching.reviewed` → HR 매칭 검토 의견 저장 시
  - `recognition.reviewed` → 경력 인정 최종 승인 시
  - `evaluation.saved` → 면접관 평가 저장 시
- `main.py`, `alembic/env.py`: audit 모듈 등록

**빌드**: Vite ✓ | Python import ✓ | Push ✓

## 다음 세션 시작점

Phase 1 Foundation Story 완료 상태:
- ✅ Story 1.1: Job Architecture (이전 세션)
- ✅ Story 1.2: Skill Library (이전 세션)
- ✅ Story 1.3: Tenant Isolation Middleware
- ✅ Story 1.4: Audit Log

다음: Story 1.5 또는 Phase 2 (Recruiting Intelligence) 진입

---

## [13차] Phase 2 Recruiting Intelligence 완성 (Stories 021-024)

### 완료 작업

**Story 021 — AI 자동 채점**
- `POST /api/v2/acquisition/applicants/{aid}/score` — Gemini 2.5-flash 기반 req_score/pref_score/ai_score/risk 산출
- `POST /api/v2/acquisition/postings/{pid}/batch-score` — 공고 내 지원자 일괄 채점
- GEMINI_API_KEY 미설정 시 키워드 overlap 비율 fallback 알고리즘
- AcqCandidates 칸반 카드에 "AI 재채점" 버튼 + 스코어 즉시 갱신

**Story 022 — 랭킹 뷰**
- `GET /api/v2/acquisition/postings/{pid}/ranked-applicants` — composite score (req×40%+pref×30%+ai×30%, risk -5)
- AcqCandidates 뷰 토글에 "AI 랭킹" 추가, 메달(🥇🥈🥉)+스코어 바(초록=필수/보라=선호/파랑=AI)+위험 뱃지

**Story 023 — 평가기준 AI 제안**
- `POST /api/v2/acquisition/criteria/suggest` — Gemini 직무 기반 필수·선호 키워드+최소경력 제안
- 직무별 fallback 딕셔너리 (품질관리/설비보전/생산관리/영업/IT)
- AcqEvaluation "✨ AI 기준 제안" 버튼 → 기존 기준과 dedup 후 머지

**Story 024 — AI 면접 질문**
- `POST /api/v2/acquisition/interviews/{iid}/questions` — 면접 컨텍스트 기반 Gemini 질문 생성(question/intent/category)
- 직무별 fallback 질문 풀
- AcqInterviews TeamInterviewView에 "✨" 버튼+질문 패널(카테고리 태그 색상 구분)
- `aiQuestionsMap` state + `handleAiQuestions` 핸들러 + 프롭 연결 완결

### 빌드 결과
- `npm run build`: 클린 통과 (2360 모듈, 2.51s)
- `python -m py_compile`: 백엔드 3개 파일 통과

### 커밋
- `3d8e038` feat: Phase 2 Recruiting Intelligence (12 files, +711/-25)
- `dd9e8c0` docs: update SESSION_LOG

### 다음 단계
- GEMINI_API_KEY 환경변수 설정 후 실제 AI 채점 테스트
- Phase 3 검토 (온보딩 자동화 등)
