# 파일맵 & 라우팅 — hr-tools-web

> 마지막 업데이트: 2026-05-10 | 소스: `frontend/src/App.jsx`

---

## 공통 컴포넌트 (`src/components/`)

| 파일 | 역할 |
|------|------|
| `Navbar.jsx` | 상단 내비바 (페이지 제목, 검색, 사용자 메뉴) |
| `AppSidebar.jsx` | 좌측 사이드바 (접힘/펼침, lucide 아이콘 NAV_ICON 맵, 섹션별 그룹) |
| `RightPanel.jsx` | 우측 패널 (최근 방문, 빠른 이동) |
| `GlobalSearch.jsx` | 전역 검색 모달 |
| `ChatbotButton.jsx` | 우하단 AI 챗봇 플로팅 버튼 |
| `ChatbotPanel.jsx` | Gemini API 챗봇 패널 |

---

## 컨텍스트 (`src/contexts/`)

| 파일 | 역할 |
|------|------|
| `AuthContext.jsx` | Firebase/JWT 로그인 상태 |
| `SidebarContext.jsx` | 사이드바 collapsed 상태 |
| `UIContext.jsx` | 최근 방문 페이지 스택 |
| `ThemeContext.jsx` | 3개 테마 (trendy-saas / apple-clean / korean-brush) 전환 |

---

## 라우트 × 컴포넌트 매핑 (`src/pages/`)

### 공개 라우트 (로그인 불필요)

| 경로 | 컴포넌트 | 설명 |
|------|----------|------|
| `/` | `Home` | 랜딩 페이지 (Hero 영상 + CTA) |
| `/login` | `Login` | 로그인 |
| `/signup` | `Signup` | 회원가입 |
| `/terms` | `Terms` | 이용약관 |
| `/acquisition` | `AcquisitionMgmt` | 확보관리 홈 (공개) |

### 확보관리 (Protected, `/acquisition/*`)

| 경로 | 컴포넌트 | 설명 |
|------|----------|------|
| `/acquisition/overview` | `AcquisitionMgmt` | 확보관리 대시보드 |
| `/acquisition/workforce` | `AcqWorkforce` | 연간 인력계획 + 채용의뢰서 |
| `/acquisition/workforce-requests` | `AcqWorkforceRequests` | 채용요청서 목록 |
| `/acquisition/recruiting/:section` | `AcqRecruiting` | 모집관리 전체 (section params로 탭 전환) |
| `/acquisition/resumes` | `AcqResumes` | 이력서 수집함 + AI 점수 분석 |
| `/acquisition/candidates` | `AcqCandidates` | 지원자 검토·선발 (서랍 패널) |
| `/acquisition/evaluation` | `AcqEvaluation` | 부서별 평가기준 설정 |
| `/acquisition/interviews` | `AcqInterviews` | 면접 파이프라인 전체 |
| `/acquisition/selection` | `AcqSelection` | 처우승인·최종합격 |
| `/acquisition/onboarding` | `AcqOnboarding` | 입사서류 검증 |
| `/acquisition/final-hire` | `AcqFinalHire` | 입사 전환 (신규입사 등록) |
| `/acquisition/data-links` | `AcqDataLinks` | 채용 데이터 연계관리 |
| `/acquisition/records` | `AcqRecords` | 채용 기록 검색·보관 |
| `/acquisition/admin-tools` | `AcqAdminTools` | 채용 운영 관리 TOOL |

### AcqRecruiting section 파라미터 목록

| section | 탭 이름 |
|---------|---------|
| `promotion-channels` | 채용사이트 계정 관리 |
| `internal-referrals` | 사내공모·사내추천 |
| `search-firms` | 써치펌·헤드헌팅 |
| `internships` | 인턴채용 관리 |
| `fixed-term` | 기간제·계약직 채용관리 |
| `job-postings` | 채용공고 관리 |
| `scan` | 지원자 자동 다운로드 (사람인 자동화) |
| `recruitment-evaluation` | 모집평가 (KPI + 퍼널) |

### HR OS (Protected)

| 경로 | 컴포넌트 | 설명 |
|------|----------|------|
| `/hr-os` | `HROperatingSystem` | HR OS 전체 개요 |
| `/organization` | `OrganizationMgmt` | 조직도 관리 |
| `/jobs` | `JobMgmt` | 직무 관리 |
| `/hr-synapse` | `HRSynapseHub` | HR Synapse (AI 지식 허브) |
| `/growth` | `HROperatingSystem` | 교육·경력개발 (moduleKey) |
| `/performance-os` | `HROperatingSystem` | 평가관리 (moduleKey) |
| `/reward` | `HROperatingSystem` | 보상관리 (moduleKey) |
| `/relations` | `HROperatingSystem` | 유지관리 (moduleKey) |
| `/separation` | `HROperatingSystem` | 이직관리 (moduleKey) |
| `/hr-analytics` | `HROperatingSystem` | HR Analytics (moduleKey) |
| `/compliance` | `HROperatingSystem` | Compliance Center (moduleKey) |

### 인사 DB (Protected)

| 경로 | 컴포넌트 | 설명 |
|------|----------|------|
| `/employees` | `EmployeeList` | 직원 목록 (인사 DB) |
| `/attendance` | `Attendance` | 근태 관리 |

### 설정 (Protected)

| 경로 | 컴포넌트 | 설명 |
|------|----------|------|
| `/settings/account` | `AccountSettings` | 계정 설정 |
| `/settings/display` | `DisplaySettings` | 화면 설정 (테마 전환) |

### Legacy redirect (→ 새 경로로)

| 구 경로 | 새 경로 |
|---------|---------|
| `/workforce-plan` | `/acquisition/workforce` |
| `/recruiting` | `/acquisition/recruiting/job-postings` |
| `/recruit-analytics` | `/acquisition/recruiting/analytics` |
| `/biodata` | `/acquisition/selection` |
| `/hire-approval` | `/acquisition/selection` |
| `/interview-mgmt` | `/acquisition/interviews` |
| `/onboarding-terms` | `/acquisition/selection` |
| `/performance` | `/performance-os` |

---

## 공유 유틸 파일 (`src/pages/`)

### acqData.js
```js
export const C = {
  cta, deep, text, muted, border, bg, soft,
  accent, accentSoft, secondary, thead   // CSS 변수 참조
}
export const STATUS = { approved, reviewing, draft, open, ... }
export const MOCK = {
  interviews, applicants, finalPassed, requisitions,
  annualPlans, resumes, channelAccounts, evaluationCriteria,
  internships, fixedTermContracts, ...
}
```

### acqStore.js (Zustand)
```js
// state: data (MOCK 기반), actions:
useAcqStore() → { data, setData, updateListItem, addListItem }
// data keys: interviews, applicants, finalPassed, requisitions,
//            annualPlans, mailLogs, offerLogs, ...
```

### DataCommandBar.jsx
```jsx
// 사용법:
<DataCommandBar
  query={q} onQueryChange={setQ}
  filters={[{ id, label, value, onChange, options }]}
  sortValue={sort} onSortChange={setSort} sortOptions={[...]}
  resultCount={n} totalCount={m}
  onReset={fn} onCsv={fn}
  noBorder   // 외부 panel 없이 내용만 렌더 (다른 카드 안에 묻을 때)
/>
// 유틸 export: uniqueOptions, includesText, compareBy, downloadCsv, ALL
```

### acqLayout.jsx 컴포넌트

| 컴포넌트 | 주요 props |
|----------|------------|
| `PageHeader` | `eyebrow, title, desc, action, icon` |
| `Panel` | `title, desc, children, aside, icon` |
| `Metric` | `label, value, unit, tone, icon, accentColor` |
| `WorkflowNav` | `title, items, activeId, onSelect, icon` (items에 `icon` 포함) |
| `PrimaryButton` | `onClick, disabled, children` |
| `SecondaryButton` | `onClick, disabled, children` |

---

## 백엔드 (`backend/`)

| 모듈 | 파일 | 역할 |
|------|------|------|
| 진입점 | `main.py` | FastAPI 앱, CORS, 라우터 등록 |
| 인증 | `auth/router.py` | 로그인/회원가입 |
| 챗봇 | `chatbot/gemini.py` | Gemini API 래퍼 |
| 채용 | `modules/acquisition/router.py` | 확보관리 API |
| 지원자 | `modules/applicants/router.py` | 지원자 CRUD + AI 점수 |
| 직원 | `modules/employees/router.py` | 인사 DB CRUD |
| 면접 | `modules/interviews/router.py` | 면접 스케줄 CRUD |
| 모집자동화 | `modules/recruiting/router.py` | 사람인 자동화 API |
| 채용공고 | `modules/recruitment/router.py` | 공고 CRUD |
| 인력계획 | `modules/workforce/router.py` | 인력계획 CRUD |

### 사람인 자동화 스크립트 (`sada/`)
| 파일 | 역할 |
|------|------|
| `pipeline.py` | 전체 파이프라인 실행 |
| `download_multi.py` | 이력서 다운로드 |
| `download_resume.py` | 이력서 단건 |
| `login_test.py` | 로그인 테스트 |
| `web_app.py` | 웹 UI 래퍼 |
